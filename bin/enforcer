#!/usr/bin/env python3
"""claude-behavior-enforcer CLI entrypoint."""

import os
import sys

# Add enforcer package to path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from enforcer.cli import main

if __name__ == "__main__":
    main()
else:
    main()
